from setuptools import setup, find_packages  
setup(name = 'pathbook', packages = find_packages())
setup(name = 'model', packages = find_packages())
setup(name = 'model.yolov7', packages = find_packages())